services:
  - type: web
    name: turfscore-api
    env: node
    buildCommand: npm install && npm run build --workspace=@turfscore/api
    startCommand: npm start --workspace=@turfscore/api
    envVars:
      - key: PORT
        value: 10000
